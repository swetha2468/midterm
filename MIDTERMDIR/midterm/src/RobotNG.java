class RobotNG extends Robot {
    // Constructor for RobotNG class, which inherits from Robot
    public RobotNG(int ID) {
        super(ID);
    }

    @Override
    public void setDirection(String direction) {
        super.setDirection(direction);
    }

    @Override
    public void advance() {
        super.advance();
        super.advance();
    }
}
